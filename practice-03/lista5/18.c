/*2. Crie um vetor de strings. Peça ao usuário que insira várias strings. Ordene as strings em
ordem alfabética utilizando qualquer algoritmo de ordenação (por exemplo, bubble sort).
Imprima as strings ordenadas.*/

#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <locale.h>